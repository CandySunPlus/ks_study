## 1. 状态管理和入口

- [x] 1.1 在 state 对象中添加 `isMemorizeMode: false` 状态标识
- [x] 1.2 创建 `startMemorize(subject)` 函数，初始化背题模式
- [x] 1.3 在 Dashboard 的每个科目卡片上添加"背题模式"按钮
- [x] 1.4 更新 `showView` 函数处理背题模式视图显示
- [x] 1.5 更新练习视图头部，在背题模式下显示"背题"标识

## 2. 选择题背题渲染

- [x] 2.1 创建 `renderMemorizeChoiceQuestion(question)` 函数
- [x] 2.2 实现直接高亮正确答案（复用 `correct` CSS 类）
- [x] 2.3 设置正确选项为已选中状态（`input.checked = true`）
- [x] 2.4 隐藏提交按钮（背题模式不需要）
- [x] 2.5 更新 `renderQuestion` 函数，根据 `isMemorizeMode` 选择渲染路径

## 3. 简答题背题渲染

- [x] 3.1 更新 `renderShortAnswerQuestion` 函数支持背题模式
- [x] 3.2 背题模式下直接显示答案，不显示"显示答案"按钮
- [x] 3.3 确保背题模式下简答题不记录 "viewed" 状态

## 4. 解析显示功能

- [x] 4.1 更新 `showFeedback` 函数，添加解析内容显示
- [x] 4.2 在 feedback 区域添加解析容器（`analysis-content`）
- [x] 4.3 创建 `renderAnalysis(question)` 辅助函数
- [x] 4.4 背题模式下在 render 时直接调用解析显示
- [x] 4.5 确保无解析的题目不显示解析区域

## 5. 错题标识

- [x] 5.1 创建 `isWrongQuestion(questionId)` 辅助函数
- [x] 5.2 背题模式下在题目标题前添加 ❌ 标识
- [x] 5.3 更新导航面板，错题格子使用 `incorrect` 样式类
- [x] 5.4 确保错题标识在题目切换时正确更新

## 6. 键盘快捷键和导航

- [x] 6.1 验证 P 键（上一题）在背题模式下工作正常
- [x] 6.2 验证 N 键（下一题）在背题模式下工作正常
- [x] 6.3 验证 Esc 键（退出）在背题模式下工作正常
- [x] 6.4 确保背题模式下 Enter 键不触发提交（因为没有提交按钮）
- [x] 6.5 确保背题模式下 A/B/C/D 键不触发选项选择

## 7. 数据隔离

- [x] 7.1 验证背题模式下不调用 `Stats.update()`
- [x] 7.2 验证背题模式下不调用 `AnsweredQuestions.set()`
- [x] 7.3 验证背题模式下不调用 `WrongAnswers.add/remove()`
- [x] 7.4 确保背题模式浏览不影响练习模式的统计数据

## 8. 测试和验证

- [x] 8.1 测试从首页进入背题模式流程
- [x] 8.2 测试选择题直接显示正确答案
- [x] 8.3 测试简答题直接显示答案
- [x] 8.4 测试解析内容在背题模式下直接显示
- [x] 8.5 测试解析内容在练习模式提交后显示
- [x] 8.6 测试错题在背题模式下的标识（标题 + 导航面板）
- [x] 8.7 测试背题模式不记录统计数据
- [x] 8.8 验证练习模式和复习模式功能未被破坏

## 9. 实现后修复

- [x] 9.1 修复：背题模式下隐藏"正确答案"提示框，只显示解析
- [x] 9.2 修复：练习/复习模式下 `showFeedback` 确保消息元素可见（`messageEl.style.display = 'block'`）
